-- jordy

ERD info:

account tabel, houdt vaste informatie over alle accounts zoals: wachtwoord en email.
de account tabel doet ook een check voor "isDocent", zodat duidelijk onstaat in de code

tickets, tickets worden gemaakt door studenten en door docenten.
students gebruiken tickets voor dagelijkse of wekelijkse plannen, docenten kunnen deze tickets checken
elke ticket heeft een tijdsindicatie, bijvoorbeeld: je maakt een ticket op maandag met je week planning, hij verloopt op vrijdag

docenten, kunnen tickets maken maar ook bekijken na verloop, kijken of een doel is behaald bvb.

admins kunnen alles zien, maakt niet uit wat, admins hebben een hogere rang dan docenten en kunnen ook verwijderde dingen van docenten zien
dus de rang order:
1. admins
2. docenten
3. studenten

ik heb de ERD onder img gezet
--


/////Alex////

The password to access the database is currently admin. The database is called croissantdb.

/////Alex////


-- jordy

wat als we php verbinden AAN een html file, dan hoef je alleen een html via browser te openen bvb

---

/////Alex////

To merge the docker containers and databases together: 
sudo docker exec -i croissant_project-db-1 mysql -u root -ppassword croissantdb < croissantdb.sql
To access the database: 
sudo docker exec -it croissant_project-db-1 mysql -u root -ppassword croissantdb

//For Jordy
docker exec -i fullstack-site1-mysql-1 mariadb -u root -ppassword forevertools < db.sql

//Windows version
//Merge
Get-Content croissantdb.sql | docker exec -i croissant_project-db-1 mysql -u root -ppassword croissantdb
//Login
Start-Process powershell -Verb RunAs -ArgumentList '-Command', 'docker exec -it croissant_project-db-1 mysql -u root -ppassword croissantdb'
/////Alex////


-- jordy

eerst een ticket kunnen maken
als je een ticket maakt moet het bij een bepaalde vak
ieder vak is verdeeld met opdrachten
als je een ticket maakt dan kun je erin zien welke opdracht je moet maken en wanneer de verloopdatum is
de aanmaaksdatum is ook zichtbaar zodat je weet vanaf en ongeveer tot wanneer je de opdracht nog kunt maken

ghgfgn
---

-- jordy

hzo is klas vervangen met class? class wordt door maar twee php files gebruikt en dat zijn ticketPage en ticketCreation
elke andere file gebruikt nog steeds klas dus de andere files zijn gebroken

---